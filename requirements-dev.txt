# Dependências para desenvolvimento local: tudo que está em requirements.txt
# + API (FastAPI/uvicorn/pydantic), testes (pytest/httpx) e plotting de notebooks
# (matplotlib/seaborn). Use:
#
#   pip install -r requirements-dev.txt

-r requirements.txt

# API
fastapi==0.115.*
uvicorn==0.32.*
pydantic==2.9.*

# Testes
pytest==8.3.*
httpx==0.27.*

# Notebooks (01_eda, 04_avaliacao_final etc.)
matplotlib==3.9.*
seaborn==0.13.*
