__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.DS_Store
*.egg-info/
.streamlit/secrets.toml

# Diretórios de tooling local (não fazem parte do produto)
.claude/
.llm/
Skill/

# dados ficam versionados (são pequenos, parte do "produto")
# models/best_model.pkl fica versionado (vencedor, ~1.2MB — produto reproduzível)
# rf_model.pkl e xgb_model.pkl são artefatos locais para a página de Comparação
# (rf_model.pkl tem ~50MB; pesado pra repo e pra memória do Streamlit Cloud);
# regenerados com `python -m src.train`
models/rf_model.pkl
models/xgb_model.pkl
